<h1>Registered users</h1>

<table>
  <tr>
    <th>Login</th>
    <th>Password</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.login %></td>
      <td><%= user.password %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Add user', new_user_path %>